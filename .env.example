# Database Configuration
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
NEXTAUTH_SECRET="your-secure-secret-key-here-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3000"

# Production Configuration Notes:
# - Change NEXTAUTH_SECRET to a strong, unique secret (use: openssl rand -base64 32)
# - Update NEXTAUTH_URL to your production domain
# - For production, consider using PostgreSQL instead of SQLite
# - Example production DATABASE_URL: "postgresql://user:password@host:port/database"
